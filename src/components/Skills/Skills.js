import styles from './Skills.module.css';

const Skills = (props) => {
  return <main id={props.id} className={styles.container}>
    <div className={styles.unknown}>
      
    </div>
  </main>
};

export default Skills;
